<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <CheckedTextView
        android:id="@+id/breakfast_selector"
        android:text="breakfast"/>
    <CheckedTextView
        android:id="@+id/lunch_selector"
        android:text="lunch"/>
    <CheckedTextView
        android:id="@+id/dinner_selector"
        android:text="dinner"/>
</merge>